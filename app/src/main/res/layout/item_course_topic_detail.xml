<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:background="@drawable/bg_dark_card"
    android:elevation="4dp"
    android:orientation="vertical"
    android:animateLayoutChanges="true">

    <!-- ─── HEADER ROW ─── -->
    <LinearLayout
        android:id="@+id/topicHeaderRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="14dp"
        android:paddingEnd="12dp"
        android:paddingTop="14dp"
        android:paddingBottom="14dp"
        android:background="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true">

        <!-- Play/Lock icon circle -->
        <FrameLayout
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/bg_stat_icon_circle"
            android:padding="7dp">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="Icono del tema"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_play_circle"
                android:tint="#9B7EFF"
                android:alpha="0.9" />
        </FrameLayout>

        <!-- Title + description -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Status badge (hidden by default, shown when active) -->
            <TextView
                android:id="@+id/topicStatusBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_status_in_progress"
                android:fontFamily="@font/sf_pro_display_semibold"
                android:includeFontPadding="false"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:layout_marginBottom="4dp"
                android:textColor="#5B8DEF"
                android:textSize="10sp"
                android:visibility="gone"
                tools:text="Tema activo"
                tools:visibility="gone" />

            <TextView
                android:id="@+id/topicNameTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/sf_pro_display_semibold"
                android:includeFontPadding="false"
                android:letterSpacing="0.01"
                android:textColor="#E8EAF6"
                android:textSize="14sp"
                android:textStyle="bold"
                tools:text="Tema 1: Fundamentos" />

            <TextView
                android:id="@+id/topicDescriptionTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:fontFamily="@font/sf_pro_display_regular"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:textColor="#6B6F88"
                android:textSize="12sp"
                tools:text="Conceptos básicos de Nested..." />

        </LinearLayout>

        <!-- Right section: progress chip + chevron + edit/delete -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Progress chip (e.g. "2/2") -->
            <TextView
                android:id="@+id/topicMetaChip"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="6dp"
                android:fontFamily="@font/sf_pro_display_semibold"
                android:includeFontPadding="false"
                android:textColor="#00D4FF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:visibility="gone"
                tools:text="2/2"
                tools:visibility="visible" />

            <!-- Edit button (creator only) -->
            <ImageButton
                android:id="@+id/editTopicButton"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Editar tema"
                android:padding="5dp"
                android:scaleType="centerInside"
                android:src="@drawable/ic_edit"
                android:tint="#9B8FA8"
                android:visibility="gone" />

            <!-- Delete button (creator only) -->
            <ImageButton
                android:id="@+id/deleteTopicButton"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Eliminar tema"
                android:padding="5dp"
                android:scaleType="centerInside"
                android:src="@drawable/ic_delete"
                android:tint="#EF4444"
                android:alpha="0.8"
                android:visibility="gone" />

            <!-- Chevron (expand/collapse) -->
            <ImageView
                android:id="@+id/topicChevron"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginStart="4dp"
                android:contentDescription="Expandir"
                android:src="@drawable/ic_keyboard_arrow_down"
                android:tint="#8B8FA8"
                android:alpha="0.7" />

        </LinearLayout>
    </LinearLayout>

    <!-- ─── EXPANDED CONTENT (only shown when expanded) ─── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="14dp"
            android:background="#0EFFFFFF" />

        <!-- Section label for videos (text removed) -->
        <TextView
            android:id="@+id/videosSectionLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="4dp"
            android:drawableStart="@drawable/ic_play_circle"
            android:drawablePadding="6dp"
            android:drawableTint="#9B7EFF"
            android:fontFamily="@font/sf_pro_display_semibold"
            android:includeFontPadding="false"
            android:letterSpacing="0.08"
            android:textColor="#9B7EFF"
            android:textSize="11sp"
            android:visibility="gone" />

        <!-- Content container (videos + files) -->
        <LinearLayout
            android:id="@+id/topicContentContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="4dp"
            android:orientation="vertical"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp" />

        <!-- ARCHIVOS section label -->
        <TextView
            android:id="@+id/filesSectionLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="4dp"
            android:drawableStart="@drawable/ic_attach_file"
            android:drawablePadding="6dp"
            android:drawableTint="#00D4FF"
            android:fontFamily="@font/sf_pro_display_semibold"
            android:includeFontPadding="false"
            android:letterSpacing="0.08"
            android:text="ARCHIVOS DEL TEMA"
            android:textColor="#00D4FF"
            android:textSize="11sp"
            android:visibility="gone" />

        <!-- Tasks container (shown on Tareas tab) -->
        <LinearLayout
            android:id="@+id/tasksDetailContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="0dp"
            android:layout_marginEnd="0dp"
            android:layout_marginBottom="4dp"
            android:orientation="vertical"
            android:paddingStart="0dp"
            android:paddingEnd="0dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:visibility="gone" />

        <View
            android:layout_width="match_parent"
            android:layout_height="4dp" />

    </LinearLayout>

</LinearLayout>
