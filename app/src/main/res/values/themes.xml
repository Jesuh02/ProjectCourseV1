<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CourseV" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        
        <!-- Force dark colors for AppBarLayout to prevent white-out on scroll -->
        <item name="appBarLayoutStyle">@style/Widget.App.AppBarLayout</item>
        <item name="collapsingToolbarLayoutStyle">@style/Widget.App.CollapsingToolbar</item>
    </style>

    <style name="Theme.CourseV" parent="Base.Theme.CourseV" />
    
    <!-- Custom AppBarLayout style to prevent white background on scroll -->
    <style name="Widget.App.AppBarLayout" parent="Widget.Material3.AppBarLayout">
        <item name="android:background">#000000</item>
        <item name="liftOnScroll">false</item>
        <item name="elevation">0dp</item>
    </style>
    
    <!-- Custom CollapsingToolbar style -->
    <style name="Widget.App.CollapsingToolbar" parent="Widget.Material3.CollapsingToolbar">
        <item name="android:background">#000000</item>
        <item name="contentScrim">#000000</item>
        <item name="statusBarScrim">#000000</item>
    </style>
    
    <!-- Dialog style for comments and other dark dialogs -->
    <style name="Theme_TareaMov_Dialog" parent="Theme.Material3.Dark.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    
    <!-- BottomSheet style for comments (Instagram/TikTok style) -->
    <style name="Theme_TareaMov_BottomSheet" parent="Theme.Material3.Dark.BottomSheetDialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="bottomSheetStyle">@style/Widget_TareaMov_BottomSheet</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
    
    <style name="Widget_TareaMov_BottomSheet" parent="Widget.Material3.BottomSheet">
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="shapeAppearance">@style/ShapeAppearance_TareaMov_BottomSheet</item>
    </style>
    
    <style name="ShapeAppearance_TareaMov_BottomSheet" parent="ShapeAppearance.Material3.Corner.ExtraLarge">
        <item name="cornerSizeTopLeft">20dp</item>
        <item name="cornerSizeTopRight">20dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>
</resources>